short flot = -3 * (a - k / 2);
int k;

while (text[uk] >= '0' && text[uk] <= '9')
	{
		if (i < MAX_CONST + 2)
			lex[i++] = text[uk++];
		else
			uk++;
	}

	if (i == MAX_CONST + 2)
	{
		PrintError("Слишком длинная константа", lex);
		return TError;
	}